#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if command -v gradle >/dev/null 2>&1; then
  exec gradle -p "$SCRIPT_DIR" "$@"
else
  echo "Gradle is required to build this project. Install Gradle 8.x or add it to PATH." >&2
  exit 1
fi
